APP_ENV=local
APP_DEBUG=true

# Database (used by Eloquent)
DB_HOST=127.0.0.1
DB_NAME=app
DB_USER=root
DB_PASS=

# Optional DSN (if you prefer DSN style)
DB_DSN=

# CORS
CORS_ALLOW_ORIGIN=*
CORS_ALLOW_METHODS=GET, POST, PUT, PATCH, DELETE, OPTIONS
CORS_ALLOW_HEADERS=Content-Type, Authorization, X-Requested-With
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=86400

# JWT
JWT_SECRET=please-change-me
JWT_ALG=HS256
JWT_ISS=quikapi
